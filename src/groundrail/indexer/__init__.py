"""Indexer: deterministic, AI-free source snapshot and unit index.

Produces ``source/snapshot.json``, ``index/file-index.json``, and
``index/unit-index.json`` with stable ids, spans, hashes, imports, and call
candidates. This is the factual ground the analyzer and router build on.
"""
